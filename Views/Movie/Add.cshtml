@model TP2_Entity.Models.Movie
<h2>Add Movie</h2>
<form asp-action="Add" method="post">
	<input type="hidden" asp-for="Id" />
	<label asp-for="Name" class="control-label">Movie Title</label>
	<input type="text" asp-for="Name" />
	<label asp-for="GenreId1" class="control-label">genre</label>
	<select asp-for="GenreId1" asp-items="@(new SelectList(ViewBag.GenreNames))">
		<option value="">---select----</option>
	</select>
	<div class="form-group">
		<input type="submit" value="add" class="btn btn-primary" />
	</div>

</form>

<style>
	form {
		display: flex !important;
		flex-direction: row !important;
		justify-content: space-evenly;
	}
</style>